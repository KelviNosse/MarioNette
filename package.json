{
  "name": "marionette",
  "version": "1.0.0",
  "description": "Getting Started with Jest and Typescript",
  "main": "index.js",
  "scripts": {
    "lint": "tslint -p . && tsc --noEmit",
    "tests": "yarn lint && jest",
    "ci": "yarn lint && yarn tests --ci"
  },
  "author": "KelviNosse",
  "license": "MIT",
  "devDependencies": {
    "@types/expect-puppeteer": "^3.3.1",
    "@types/jest": "^24.0.15",
    "@types/jest-environment-puppeteer": "^4.0.0",
    "@types/node": "12.6.2",
    "@types/puppeteer": "^1.12.4",
    "jest": "^24.8.0",
    "jest-puppeteer": "^4.2.0",
    "puppeteer": "^1.18.1",
    "ts-jest": "^24.0.2",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  }
}
